<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.electricity.mapper.UserMapper">

    <resultMap id="UserResultMap" type="User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="fullName" column="full_name"/>
        <result property="email" column="email"/>
        <result property="roleId" column="role_id"/>
        <result property="enabled" column="enabled"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="roleName" column="role_name"/>
    </resultMap>

    <sql id="selectBase">
        SELECT u.*, r.name AS role_name
        FROM users u
        LEFT JOIN roles r ON u.role_id = r.id
    </sql>

    <select id="findById" resultMap="UserResultMap">
        <include refid="selectBase"/>
        WHERE u.id = #{id}
    </select>

    <select id="findByUsername" resultMap="UserResultMap">
        <include refid="selectBase"/>
        WHERE u.username = #{username}
    </select>

    <select id="findAll" resultMap="UserResultMap">
        <include refid="selectBase"/>
        ORDER BY u.username
    </select>

    <select id="findAllPaged" resultMap="UserResultMap">
        <include refid="selectBase"/>
        <where>
            <if test="search != null and search != ''">
                AND (u.username LIKE CONCAT('%',#{search},'%')
                  OR u.full_name LIKE CONCAT('%',#{search},'%')
                  OR u.email LIKE CONCAT('%',#{search},'%'))
            </if>
        </where>
        ORDER BY u.username
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM users u
        <where>
            <if test="search != null and search != ''">
                AND (u.username LIKE CONCAT('%',#{search},'%')
                  OR u.full_name LIKE CONCAT('%',#{search},'%')
                  OR u.email LIKE CONCAT('%',#{search},'%'))
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, full_name, email, role_id, enabled, created_at, updated_at)
        VALUES (#{username}, #{password}, #{fullName}, #{email}, #{roleId}, #{enabled}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE users SET
            username = #{username},
            full_name = #{fullName},
            email = #{email},
            role_id = #{roleId},
            enabled = #{enabled},
            <if test="password != null">password = #{password},</if>
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>
