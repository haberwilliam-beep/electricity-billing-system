<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.electricity.mapper.AuditLogMapper">

    <resultMap id="auditResultMap" type="AuditLog">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="action" column="action"/>
        <result property="entityType" column="entity_type"/>
        <result property="entityId" column="entity_id"/>
        <result property="description" column="description"/>
        <result property="ipAddress" column="ip_address"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO audit_logs (user_id, username, action, entity_type, entity_id, description, ip_address)
        VALUES (#{userId}, #{username}, #{action}, #{entityType}, #{entityId}, #{description}, #{ipAddress})
    </insert>

    <select id="findAllPaged" resultMap="auditResultMap">
        SELECT * FROM audit_logs
        <where>
            <if test="search != null and search != ''">
                AND (username LIKE CONCAT('%',#{search},'%')
                  OR action LIKE CONCAT('%',#{search},'%')
                  OR entity_type LIKE CONCAT('%',#{search},'%')
                  OR description LIKE CONCAT('%',#{search},'%'))
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM audit_logs
        <where>
            <if test="search != null and search != ''">
                AND (username LIKE CONCAT('%',#{search},'%')
                  OR action LIKE CONCAT('%',#{search},'%')
                  OR entity_type LIKE CONCAT('%',#{search},'%')
                  OR description LIKE CONCAT('%',#{search},'%'))
            </if>
        </where>
    </select>
</mapper>
